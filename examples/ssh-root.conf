# SSH root key import config for debian-network-tui
# Place this file next to the debian-network-tui binary.
#
# Path to an OpenSSH public key file (relative to this directory, or absolute).
PubkeyFile=root.pub
